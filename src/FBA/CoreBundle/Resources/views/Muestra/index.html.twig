{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Muestra list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Valorresultado</th>
                <th>Interpretacion</th>
                <th>Decision</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('muestra_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.valorResultado }}</td>
                <td>{{ entity.interpretacion }}</td>
                <td>{{ entity.decision }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('muestra_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('muestra_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('muestra_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
