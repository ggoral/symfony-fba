{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Resultado</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Fecharecepcion</th>
                <td>{{ entity.fechaRecepcion|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Fechaanalisis</th>
                <td>{{ entity.fechaAnalisis|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Fechaingreso</th>
                <td>{{ entity.fechaIngreso|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('resultado') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('resultado_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
