{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Resultado list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Fecharecepcion</th>
                <th>Fechaanalisis</th>
                <th>Fechaingreso</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('resultado_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.fechaRecepcion %}{{ entity.fechaRecepcion|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.fechaAnalisis %}{{ entity.fechaAnalisis|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.fechaIngreso %}{{ entity.fechaIngreso|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('resultado_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('resultado_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('resultado_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
